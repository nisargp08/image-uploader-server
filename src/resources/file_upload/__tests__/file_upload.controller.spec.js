import * as controller from '../file_upload.controller';

describe('File upload', () => {
    test('can upload a file', () => {
        // Get a file
        const imageFile = './test_image_upload.jpeg';
        // Check if file is of type image
        // Check if file size is less than 10MB
        // Upload the file
        // Check if uploaded file is present in the output directory or not
    })
})